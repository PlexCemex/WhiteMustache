# 🥸 WhiteMustache

HTML файлы для сайта **Подработок и работ студентов ДВФУ**, хранятся в папке **`Current html files of project/`**

---

## 📡 Общие параметры

**Endpoint:** `localhost:80/Job/ws/JobService.1cws`  
**Формат:** SOAP XML

---

## 📄 HTML Страницы

### `vacancies.html`
**Назначение:** Главная страница для студентов с поиском и фильтрацией вакансий подработок

**Функциональность:**
- Список всех доступных вакансий
- Фильтрация по направлениям работ
- Просмотр деталей вакансии (зарплата, даты, описание)
- Форма отклика на вакансию через модальное окно

**API Endpoints:**
- `GET /JobService/hs/jobservice/vacancylist/` — получить список вакансий
- `GET /JobService/hs/jobservice/tags` — получить список направлений работ
- `POST /JobService/hs/jobservice/request` — отправить отклик на вакансию

**Данные для отклика:**
- `student` (строка) — СНИЛС студента
- `startperiod` (строка) — дата начала работы (формат: YYYYMMDD)
- `endperiod` (строка) — дата окончания работы (формат: YYYYMMDD)
- `description` (строка) — информация о студенте и опыте
- `vacancy` (строка) — номер вакансии

---

### `employer.html`
**Назначение:** Кабинет работодателя для управления вакансиями и просмотра откликов

**Функциональность:**
- Список собственных вакансий работодателя
- Создание новых вакансий через модальную форму
- Просмотр и управление откликами на вакансии
- Форма с выбором направлений работ

**API Endpoints:**
- `GET /JobService/hs/jobservice/vacancylist/` — получить список вакансий (с фильтром по организации)
- `GET /JobService/hs/jobservice/tags` — получить список направлений работ
- `POST /JobService/hs/jobservice/vacancy` — создать новую вакансию
- `POST /JobService/hs/jobservice/requestlist` — получить отклики на вакансию

**Данные для создания вакансии:**
- `title` (строка) — название должности
- `salary` (число) — размер зарплаты
- `dateofbegin` (строка) — дата начала приёма (формат: YYYYMMDD)
- `dateofend` (строка) — дата окончания приёма (формат: YYYYMMDD)
- `description` (строка) — описание работы и требования
- `organization` (строка) — ID организации
- `typesofwork` (строка) — направления работ (через запятую)

---

## 🔧 Mock Server

Go сервер на порту 8080 обрабатывает SOAP запросы, логирует данные и отправляет успешный ответ.