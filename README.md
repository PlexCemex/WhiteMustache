# 🥸 WhiteMustache

HTML формы для сбора данных о вакансиях и отправки их через SOAP на бекенд.

## 📁 HTML Страницы

Все HTML файлы находятся в папке **`Current html files of project/`**

### Форма сбора данных

На странице собираются следующие данные:
- **Organization** — организация
- **Description** — описание вакансии
- **DateOfBegin** — дата начала
- **DateOfEnd** — дата окончания
- **TypesOfWork** — типы работ

Данные каждого поля отправляются в виде строк через SOAP (XML) на бекенд для сохранения.

### Отправка данных

**Endpoint:** `localhost:8080/Job/ws/JobService.1cws`  
**Метод:** `PutVacancy`  
**Формат:** XML SOAP

## 🔧 Mock Server

Go сервер на порту 8080 обрабатывает SOAP-запросы, логирует данные и отправляет успешный ответ.

---

**На борту:** HTML · SOAP/XML · Go Backend