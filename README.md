# 🥸 WhiteMustache

Mock SOAP server для обработки запросов вакансий в XML формате.

## 📁 Структура проекта

### HTML файлы
Все HTML страницы находятся в папке **`Current html files of project/`** — используй их как фронтенд-часть проекта.

## 🔧 Как это работает

### Mock Server
Go сервер на порту **8080** обрабатывает SOAP-запросы:

- **Endpoint:** `/Job/ws/JobService.1cws`
- **Метод:** POST
- **Формат:** XML (SOAP Envelope)
- **CORS:** Включен для всех источников

Сервер парсит входящий XML, логирует данные в консоль и отправляет успешный ответ.

### Что сервер обрабатывает

```xml
<Envelope>
  <Body>
    <PutVacancy>
      <Organization>...</Organization>
      <Description>...</Description>
      <DateOfBegin>...</DateOfBegin>
      <DateOfEnd>...</DateOfEnd>
      <TypesOfWork>...</TypesOfWork>
    </PutVacancy>
  </Body>
</Envelope>
```

## 🚀 Быстрый старт

### Локальный запуск

```bash
# Собрать бинарник
make build

# Запустить сервер
./mock-server/main

# Будет доступен на http://localhost:8080
```

### Docker

```bash
# Собрать образ
make docker-build

# Запустить контейнер
docker run -p 8080:8080 plexcemex/whitemustache:latest
```

## 📦 Деплой

```bash
# Собрать и залить в Docker Hub
make docker-push
```

---

**На борту:** Go · Docker · SOAP/XML · CORS Middleware