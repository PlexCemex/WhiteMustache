# 🥸 WhiteMustache

HTML файлы для сайта **Подработок и работ студентов ДВФУ**

---

## 📄 HTML Страницы

### `Vacancy-Form.html`
**Назначение:** Форма для размещения новой вакансии-подработки студентом или работодателем

**Endpoint:** `localhost:8080/Job/ws/JobService.1cws`  
**Метод:** `PutVacancy`  
**Формат:** SOAP XML

**Данные, отправляемые на бэкенд:**
- `Organization` (строка) — название организации/компании
- `Description` (строка) — описание работы и требования
- `DateOfBegin` (строка) — дата начала работы
- `DateOfEnd` (строка) — дата окончания работы
- `TypesOfWork` (строка) — типы работ (выбирается несколько вариантов, отправляется через запятую)

---

## 🔧 Mock Server

Go сервер на порту 8080 обрабатывает SOAP запросы, логирует данные и отправляет успешный ответ.

